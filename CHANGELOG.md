# 更新日誌

本專案所有值得記錄的變更都寫在這個檔案。

格式依循 [Keep a Changelog](https://keepachangelog.com/zh-TW/1.1.0/)，
版本編號依循 [語意化版本](https://semver.org/lang/zh-TW/)。

## [未發布]

## [0.7.0] - 2026-09-20

前六版處理的都是「一本刊怎麼記」。這一版第一次把重量壓到**另一端**：文章講的那款遊戲怎麼記。站上的 `Game` 原本只是文章掛得上去的一個名字——一列 name 加 slug，沒有平台、沒有封面、沒有站外出處，同一款遊戲散成三四列也沒有工具查得出來。這一版給它平台欄位、把中文 DOS 遊戲資料庫與維基百科接成站外連結，並做出一整套**先量再改**的稽核腳本：重複、假名、括號英文、孤兒條目各自出一份 CSV，清理完可以再跑一次驗收。

另外多了一頁 **年代軸**，把 1986 年以來的創刊、改名與停刊，跟主機上市、作業系統改版與著作權法修正排在同一條時間線上——單看一本刊的沿革回答不了「為什麼 1998 年一口氣冒出七本雜誌」。

資料端是一整輪的匯入與考證：站上從 44 本刊、2523 期、10438 篇長到 **56 本刊、2765 期、13940 篇、6754 款遊戲**，其中 1353 款填了平台、1995 條站外連結。

### 新增

- **年代軸 `/timeline`**——雜誌依刊種分欄排成軌道，左欄是外部事件。**每一筆事件都要有 `source`**，沒有出處的條目就是站上憑空多出來的斷言，寧可不放；查到相衝的說法時兩邊都寫進 `note`，不挑一個看起來順的。主機記的是**台灣**買得到的日子、作業系統記的是**繁體中文版**上市——雜誌報導的是讀者手上的機器。另有幾筆「時代座標」（首任民選總統、香港主權移交、921、911）刻意不寫它與雜誌的關係，那個關係要查才知道
- **`Game.platforms`**——代號是**純代號**，沒有斜線也沒有中文別名。這條規矩是被 `PLATFORM` 標籤逼出來的：那邊沒收斂過，`SFC`／`SFC/超任`／`超級任天堂` 三種寫法指同一台。**存細的、顯示粗的**——`DOS`／`WIN`／`PC98`／`APPLE2` 各自存著，前台收斂成一枚「PC」；這個站只需要 PC，但統一資料庫那一邊需要細的，而寫入時收斂省不了多少事卻再也回不去
- **遊戲頁的站外連結**——`ExternalLink` 從雜誌／單期擴到遊戲，接上中文 DOS 遊戲資料庫與維基百科
- **連結顯示的是條目名，不只站名**——一整排「維基百科」「維基百科」說得出是哪個站，說不出點進去會看到什麼。維基的條目名**從網址讀**（那 555 條是整批建的，沒有人一條一條填過名字），中文 DOS 遊戲資料庫的網址是流水號、只能靠回填。與本頁同名時不印：標題「神奇王國」底下再寫一次「中文 DOS 遊戲資料庫 · 神奇王國」，讀者得到的資訊是零。**比對一字不差，不把括號正規化掉**——上游標題裡的括號正是同名遊戲之間唯一的區別
- **遊戲索引的卡片改成 3:4 封面槽**——盒裝圖裁成方框會失去標題
- **圖片 lightbox**——雜誌圖庫、單期的額外圖片與目錄圖共用
- **雜誌分類加「手機遊戲」**——前三類沿用 nostalibrary 的分節，這一類是站上自己加的：上游收的年代還沒有手遊，而東立 2017 年的《Passion》與《ai愛少女》報導的全是手遊，硬歸進「線上遊戲」會讓那個篩選不可信
- **雜誌列表加 ISSN 欄與依期數排序**
- **書稿資料 `docs/book/`**——《台灣遊戲雜誌圖鑑》（暫名）一篇一份資料卡：沿革、可寫的話題、圖版候選、資料缺口與出處。**這裡不擬稿**，正文由人來寫
- **遊戲資料的稽核與清理腳本**——[`audit-games.ts`](scripts/audit-games.ts) 把問題分成重複、假名、超長、括號英文、孤兒五類各出一份 CSV，清理量得出來；[`match-cdosgame.ts`](scripts/match-cdosgame.ts) 比對上游條目並分桶列出合併候選；[`suggest-platforms.ts`](scripts/suggest-platforms.ts)／[`apply-platforms.ts`](scripts/apply-platforms.ts) 從兩個來源提議平台再套用；[`enrich-from-cdosgame.ts`](scripts/enrich-from-cdosgame.ts) 補代表性條目的欄位與封面；[`link-cdosgame.ts`](scripts/link-cdosgame.ts) 建站外連結並回填條目名。全部走 API、冪等

### 變更

- **OCR 關掉模型的思考模式**——同一張目錄圖（《軟體世界》60 期，人工複查 66 篇），開著思考只辨識出 27–33 篇，關掉就是 66 篇。差別不在快慢，在於思考會把「逐條轉錄」做成「摘要挑選」；調 `reasoning_effort` 的三個檔位都落在 27–33。判斷有沒有生效看回應裡有沒有 `reasoning_content`，症狀是辨識量無預警砍半
- **OCR 固定 `temperature: 0` 並改走串流**——轉錄只有一個正確答案，取樣自由度換不到東西卻折損召回（0.1 時同一張掃描兩次跑出 37 篇與 51 篇）。串流是為了讓回應一直有東西流出來：自架後端在 Cloudflare 後面，那個 100 秒上限任何方案都改不了
- **表格預設換行，不換行由欄位自己指定**——shadcn 原本給每一格 `whitespace-nowrap`，在只裝短值的後台表格看不出問題，但只要有一欄裝自由文字就撐到 max-content、整張表超出容器。翻過來之後，「這一欄不能斷」是個看得見的決定而不是繼承來的；需要保證不斷行的 25 處各自補上，散在 8 支檔案
- **賣場來源的出處只留名字、不連結**——拍賣、購物平台、網路書店連出去像在替賣場導流，而商品下架後那個連結本來也活不久。文章、論壇、部落格不受影響。判斷做在頁面組資料那一步，不留到元件裡：值只要進了 props 就會落在 RSC payload，等於「點不到」但仍然公開
- **合併遊戲會把輸的那一列的欄位帶過去**——原本只搬關聯，欄位跟著那一列一起消失
- **首頁只收有封面的單期，並在總數底下加一行涵蓋數**——「其中 N 期有封面、M 期已錄入目錄」，都相對於單期總數講。卡片幾乎就是封面，少一張讀起來是列上的一個洞
- **單期「封面資訊」的 credit label 改由值決定**——原本一律寫「封面」，在已經叫「封面資訊」的 box 裡說了等於沒說；自帶角色詞的值還會讀成「封面：攝影：…」兩個冒號
- **描述與備註一行一段**——那兩欄是寫給讀者看的，不是編輯的草稿紙
- **GA 不再記錄後台**——`/admin` 的連結改走硬導航，軟導航會讓 gtag.js 一路掛著、把後台頁面報成 page_view

### 修正

- **OCR 在代理逾時前先寫出第一個位元組**——密的目錄頁跑 65 秒、病態的超過 150 秒，原本會被中途切斷、在結果裡挖一個無聲的洞。代價是**狀態碼不再說明成敗**：凡是送到模型的都回 200，要看 body 的 `result` 或 `error`
- **維基連結的語言變體路徑讀不出條目名**——只認 `/wiki/` 的話，走 `/zh-tw/`（26 條）與 `/zh-hant/`（15 條）的那 41 條會退回只顯示站名
- **中文 DOS 遊戲資料庫的維基參照取錯**——取的是頁面上第一個維基連結，不是掛在該款遊戲上的那一個
- **遊戲頁的文章表格撐出水平捲軸**——量到 viewport 820px 時表格 1568px、容器 694px。根因是上面那條 `TableCell` 預設，不是欄寬沒設上限
- **遊戲索引卡片比內容高出 42px**——`Card` 自己的 `py-5`。縮圖還是方框時看不太出來，換成 3:4 之後那段留白就明顯了
- **《電玩通》早期五期的目錄頁碼錯位**——OCR 把欄目底下的子項目全給了欄目那一頁。用封底裡的索引回填 29 篇，接法是標題與索引名逐字相同、且索引在該期只指向一個頁碼才改
- **《電玩通》封面人物的姓名空格**——起因是拿目錄頁的 COVER STAFF 欄去校正封面，但那兩處對日本藝人的印法系統性不同，本來就不該互相校正。規則定案為照封面印的
- **維基連結指向的不是那款遊戲**——47 條指向廠商條目或遊戲清單（`ニホンクリエイト`、`List of PC-98 games` 之類）已清掉。它們一直都在，是「連結顯示條目名」之後才看得出來
- **《軟體世界》111 期整本重複匯入**——121 篇收成 60 篇，六個標題重複了三次；刪除前先驗過每一筆要刪的與保留的完全相同、關聯也是子集

## [0.6.0] - 2026-09-06

上一版處理「同一本刊叫什麼名字」，這一版處理的是**一本刊、一冊雜誌還有什麼沒地方放**。發刊多久一次、這一冊算本刊還是試刊還是特刊、封面上畫了什麼又是誰做的、額外的照片、站外查得到的資料、這本刊已知總共出過幾期——先前這些不是擠在 `notes` 或 `description` 裡靠人讀，就是根本記不下來。欄位補齊之後，「收錄 N 期」這種一直在顯示卻沒有定義的數字，終於說得出它在數什麼。

前台跟著補上瀏覽的細節，資料端則是一整輪的匯入與考證：站上從 42 本刊、2242 期長到 **44 本刊、2523 期、10438 篇文章**。

### 新增

- **額外圖片（`Photo`）**——掛雜誌或掛單期二擇一，帶說明、來源與 `isPublic`。公開頁**濾在查詢層**，不濾在畫面層——後者等於把未公開的網址一起送到瀏覽器。後台各多一個圖片區塊，走 `/api/photos` 即時存檔並留 `EditLog`
- **站外資訊連結（`ExternalLink`）**——Internet Archive 的全本掃描、上游條目、國圖書目、維基條目，與 `Photo` 同形二擇一。**類型記站點不記用途**：站點決定顯示名稱，編輯不必打字
- **發刊頻率（`Magazine.frequency`）**——單一值不是複選。頻率是時序屬性，一本刊在任何一個時間點只有一種，複選既說不出先後也說不出主次。存創刊值，中途改變寫進 `description`
- **刊種（`Issue.kind`：本刊／試刊／特刊）**——站上的「收錄 N 期」終於有定義。原本把試刊、特刊、正刊混著數，卻和來源抄來的已知總期數並排顯示，差額只能配一句手寫警語。**判準只看封面上的編號**：進入正刊序列的是本刊，另立名目的與正式創刊前的都不是，所以兩個編輯會得到同一個答案
- **封面資訊三欄**（`coverGames`／`coverSubjects`／`coverCredit`）——被畫的那端是封閉集合（遊戲、真人），製作的那端是開放集合（繪師、攝影、設計⋯⋯），而開放集合放進欄位名就是無底洞。所以角色種類寫在**值**裡（「攝影：陳某」），加一種角色不需要 migration
- **已知總期數**（`knownIssueCount`／`knownIssueCountSource`）——記已知不記應有，查不到就留空。列表顯示成「收錄 / 已知」，掛刊系不掛刊名時期
- **公開頁的「已校訂」**——完備原本只有 ADMIN 看得見；公開端收成兩態、換一個詞，API 回衍生的布林 `isVerified` 而不回時間戳
- **單期頁的上一期／下一期**——用 `order` 找，不是期號加一：期號排不出前後（`vol.01`、`試刊號`、`70+71` 之間沒有大小），也不能用 `order ± 1`，合併號各佔一格而站上不是每一期都建了
- **`/games` 與 `/tags` 可依欄位排序，刊期欄帶封面預覽**——預覽用 Popover 走 Portal 掛在 `body` 上；表格容器是 `overflow-x-auto`，CSS 規範下 `overflow-y` 的 `visible` 會被算成 `auto`，純 CSS 的 hover 卡片會被裁掉
- **404 改成一頁雜誌目錄**——每個出口是一列專欄名、點線與頁碼，最後一條固定是「這一頁 …… 404」。沒有對應頁面的專欄渲染成「本期休刊」，日後補上網址就會自己亮起來
- **後台單期編輯頁連得到前台單期頁**——那條網址是雜誌 slug 加單期 slug 兩段拼的，而單期 slug 未必是期號，記不住也猜不出
- **Search Console 的三條驗證並存**（DNS TXT、`public/` 的 HTML 檔、HTML 標記），任一條斷了還有別的撐著
- **腳本**——[`scripts/backfill-issue-kind.ts`](scripts/backfill-issue-kind.ts) 回填刊種，走 API、冪等、只往非 `REGULAR` 改，且**只讀期號不讀標題**（讀標題會把《軟體世界》70+71、《電擊王》Vol.12 誤判成特刊，一筆真陽性都沒多抓到）

### 變更

- **期號改存純數字**——前綴形狀是呈現不是資料，而且封面判不出來：《電視遊樂雜誌》第 28 期封面印 `NO 28`（沒有點）、第 31 期印 `NO. 31`（有點），補零更不是封面來的。顯示時由 `formatIssueNumber()` 補成「第 N 期」；前綴承載語意時才留。正式站照這條收斂 **1424 期**
- **年份＋卷號的 slug 加 `v`**——`1999-11` 與 `YYYY-MM` 同形，22 期會被誤讀成年月；改成 `1999-v11`
- **描述與本期資訊裡的網址渲染成連結**——難的不是找出網址，是決定它在哪裡結束：這些欄位是中文寫的，網址後面幾乎一定跟著全形標點。比對時把 CJK 與全形字元排除在字元集外，再從尾端剝掉半形句讀，右括號要數過才剝
- **「已校訂」的標記改成一枚紅印**——三個字紅底白字太顯眼，長度一長就從標記變成第二個重點。中途試過楷體單字「校」，但 Safari 只暴露固定的系統字體清單、楷體不在裡面，於是改成把紅底與圖示畫在同一個 SVG 座標系，任何尺寸都同心，也不依賴任何字體
- **首頁副標拿掉「日本」**——站上目前收的都是台灣雜誌
- **收錄數可以多於已知總期數**，公開頁在較大時補一句說明。那句話只陳述事實，不寫成「增刊一律不計入」——算不算增刊是各來源自己的事
- **表單 placeholder 改用淺色斜體**，停刊日的範例改用不可能的日期，不再像已填的真值
- **`BACKLOG.md` 拆成兩層**——602 行裡有十條佔掉 223 行，那些不是隨手記而是決策背後的考證。十四條搬進 `docs/backlog/<slug>.md`，主檔留一行加連結，回到 140 行、一個畫面掃得完
- **OCR 提示詞的系列標籤寫法**——`型別:名稱` 是文件裡的寫法不是 `Tag.name` 的內容，而提示詞照著它要求模型輸出，正式站因此長出 `SERIES:創世紀系列` 這類標籤

### 修正

- **分頁會讓同一列出現在兩頁、另一列被跳過**——`Issue.order` 每本刊各自從 0 起算，全站掃描時大量並列，而 offset 分頁在非全序上不成立（2306 列回傳、2282 個相異 id）。`/api/issues`、`/api/articles` 與共用的 `gameOrderBy()` 都補上 `id` 當決勝欄
- **同時送出的重名使用者**——對 `lower(name)` 建 partial unique index
- **CSV 備份少了只存在於後台的欄位**，並加 schema 覆蓋測試，日後加欄位會被測出來
- **《電視遊樂雜誌》第 28–33 期整段錯掛一期**——站上 28／31／32 掛的分別是 29／32／33 的封面與出刊日。blob 檔名本來就寫著正確的期號，所以是掛錯期不是傳錯檔
- **匯入腳本擋掉的四類資料**——沒有出版日期的期（`allowMissingDate` 早就存在，只是有些刊的規則沒寫上那一行）、季度精度的日期（`1994 夏` → EDTF `1994-22`，不虛構月份）、非數字的價格（`贈閱` 進 `notes`，免費贈閱的仍然是雜誌）、以及上游 `id` 欄空白時推導不出封面檔名

## [0.5.0] - 2026-08-24

上一版讓站台「值得被讀」，這一版處理的是**同一本刊在不同時間叫不同名字**這件事。雜誌會改名、封面上同時印著兩種語言的刊名、翻譯刊還揹著原刊的名字——先前這些全擠在一個 `aliases` 陣列裡，於是誰也查不出什麼。現在四種名字各有各的欄位、改名切成刊名時期、網址代號依規範重算並保住舊連結。

### 新增

- **刊名沿革**——一本刊改名不拆成兩筆。每個題名穩定的連續階段建一段 `MagazineTitle`，只記「從哪一期起用這個名字」，一段的終點就是下一段起點的前一期。公開站把時期當成自己的雜誌卡片列出、單期列表以時期分帶並在標題下標出前身；後台可編輯沿革，起始期以可篩選的下拉挑選。**歸併與切分判準寫進了 [`docs/data-conventions.md`](docs/data-conventions.md)**：什麼證據足以判定兩段是同一條刊系（期號序列最強，ISSN 不列入），以及什麼變更值得切一段（正題名有語意變動才切，刊期變更不切）
- **舊網址不會因為改代號而斷**——雜誌換 slug 時舊值退役進 `MagazineSlug`，`/magazines/<舊值>` 與它底下每一條單期網址都 308 轉到現行值。退役值同時是**佔位**：別本刊撿不走，否則舊連結會靜悄悄指到另一本雜誌
- **`llms.txt` 與 AI 爬蟲政策**——政策寫成一個明確的決定而不是預設值
- **Open Graph 與 schema.org 結構化資料**——分享出去的連結不再是一條裸網址，機器也讀得到雜誌與單期是什麼
- **站台分析**——只量公開站的讀者，不量我們自己的編輯行為
- **站台自己的 404 頁**，取代 Next.js 的預設頁
- **單期的完備標記**——記錄這一期的資料是否已經建齊，並在事後被改動時察覺
- **列表可切換成橫列版面**，分類 chip 縮成帶色的短碼（線上遊戲用圈內慣用的 OLG）
- **缺封面的單期顯示一張畫出「缺什麼」的插圖**，而不是一個破圖框
- **後台**——分頁可直接輸入頁碼並顯示總頁數、標籤列表不再只到第 100 筆、遊戲列表可排序也可依平台與類型篩選、期號可直接點開該期、單期表單移到文章列表旁邊而不是壓在上面
- **腳本**——季度還原檢查收斂成一道指令、列出資料庫沒有任何一列指向的孤兒 blob、把《電玩通》PlayStation 家族與《遊戲設計大師》《電擊王》《電遊人》從 Sheet 匯入，以及依規範重算雜誌 slug 的 [`scripts/apply-magazine-slugs.ts`](scripts/apply-magazine-slugs.ts)
- **`data/collectors-note.txt`**——一位收藏家的「全期數已收齊」清單，21 本刊的創刊、休刊、期名與本數。拿來比對站上缺哪些刊：目前確認缺 6 本

### 變更

- **雜誌名稱拆成四個語意明確的欄位**——`nameParallel`（封面上與正題名並列的另一語言刊名）、`sourceTitle`（本刊整體翻譯自哪本外刊）、`MagazineTitle`（改名後的名字）、`aliases`（其餘）。先前只有一個 `nameOriginal`，它同時裝著自印英文名、原刊名、改名後的名字與俗稱，四種關係混在一起的結果是查詢查不出東西——**也讓 slug 規範一度把 `gwalker`、`vvkids` 誤讀成編輯憑空意譯，實際上都印在封面上**
- **雜誌 slug 依規範重算**，34 本中的 29 本換了值（`fmt-tw` → `famitsu-tw`、`gd` → `game-developer`、`jxdn` → `jingxun-diannao`）。規範的核心是**每一段都要指得出它印在哪裡**：走並列刊名、原刊通稱 + `-tw`、或創刊名的漢語拼音，先命中先用。它一次否決了現況裡 24 個編輯自造、無從推導的值。設計與 34 本的對照表見 [`docs/plans/2026-08-23-magazine-slug-convention-design.md`](docs/plans/2026-08-23-magazine-slug-convention-design.md)
- **畫面上一律稱「雜誌」不稱「期刊」**
- **單期可以沒有出版日期**——一輪的排序改用 `order`。填不出來就留空，那不是缺陷是誠實
- **`publishDate` 的定義寫進慣例**——記的是版權頁宣告的出版日，**不是實際上市日，也不是封面那個長得像日期的期次標示**
- **裸期號顯示成「第 216 期」**，`創刊號`、`70+71` 這類原樣輸出
- **RAWG 先以英文名查詢**，結果由編輯挑選而非自動採用，挑錯可以還原
- OCR 的流量限制器縮到它實際在做的事；文章與遊戲、文章與標籤的關聯補上反向索引

### 修正

- **AI 辨識讀不懂模型的回答時會說出來**——模型偶爾漏掉一個字串值的開頭引號（`"title":囂張拳王",`），解析失敗被吞成空的文章清單，看起來就像「這張目錄頁上什麼都沒有」。軟體世界 35、36 期各重跑了七次都是這個樣子。現在解析失敗回 422 並帶上原因，辨識紀錄照樣寫入以保留原始回應；另外對這個壞法補一層修復後重試，拿 35 期存下來的那筆回應可救回全部 38 篇
- **單期複查標記不再被自己的表單清掉**——編輯頁的表單以 props 判斷「這位編輯有沒有動過複查勾選」，但那個判斷即時更新、勾選框卻停在載入當下的值。文章列表上的「標記為已複查」寫入後，下一次儲存就會把它寫回未複查。編輯紀錄上有十五對這樣的痕跡
- **「單期複查」列表在 Safari 點哪一列都開到同一期**——整列的點擊區是靠 `position: relative` 的 `<tr>` 撐開一層 `::after`，而 CSS 2.1 對表格列的相對定位行為未定義，WebKit 不把該列當成包含區塊，於是最後一列的遮罩蓋住整張表。連結收回成「雜誌名 + 期號」那串字本身
- **特輯標題不再把操作按鈕擠出畫面**——電腦玩家 216 期的標題有 48 個字，欄位沒有寬度上限也無處換行，整張表格被撐得超出容器，所有列的編輯與刪除鈕都被推到右邊看不見。300px 是上限而非固定寬：欄位吃其他欄剩下的空間，視窗不夠寬時繼續縮，超出以刪節號表示、完整內容留在 tooltip。操作欄另外釘在捲動容器右緣（`sticky right-0`），窄視窗下表格仍可橫捲，但按鈕不會跟著捲走
- **逗號分隔的欄位只存得下一個值**——別名、其他編號、作者這幾個輸入框各自抄了一份 `split(",")`，其中一份漏了展開，於是打幾個都只留第一個
- **middleware 只認得三個 ADMIN 專屬頁面裡的兩個**
- **匯出漏掉表單不再顯示的欄位**——備份的職責是原樣帶走，不跟著介面走
- **CSV 範本給了一個解析器從來沒讀過的欄位**
- **篩選 chip 的計數以顯示單位計**——先前數的是資料列，與 chip 旁邊那個數字對不起來
- **「同一個名字」只留一把尺**，辨識與搜尋共用，不再各判各的
- 後台的隱藏 label 讓視窗多出第二條捲軸；雜誌表單以自己那欄的寬度反應而非視窗寬度；貢獻者動態連到工作本身而不是編輯表單

## [0.4.0] - 2026-08-20

這一版把站從「後台建得了資料」推到「前台值得被讀」：整站換上可讀的網址、期刊與遊戲各自有了瀏覽介面、目錄複查併回單期編輯頁一氣呵成，並補上這個站原本完全沒有的兩件事——**備份**，與**讓搜尋引擎找得到**。

### 新增

- **可讀的公開網址**——遊戲與標籤改以 slug 為網址（`/games/信長之野望`），期刊與單期則是 `/magazines/ace/issues/163`。**期刊 slug 限定 ASCII 而遊戲用中文**：期刊 slug 是每條單期網址的前綴，中文擺在這個位置會把站上最常被分享的網址整條 percent-encode。舊的 cuid 網址不會斷，找不到 slug 就用 id 查，命中就 308 轉址。NFKC 正規化擺在產生規則的最前面，否則 `宇宙傳奇Ⅱ` 與 `宇宙傳奇Ⅲ` 的羅馬數字會被當標點吃掉，兩款續作撞成同一個 slug
- **單期的永久短碼**——`/i/<code>` 是唯一不會變的握把：期刊會改名、期號會重排、slug 可以重填，正規網址的每一段都可能動，但這條連結不會。用 307 而非 308，因為目的地本來就會變
- **統一的目錄複查流程**——辨識完直接落地成文章並跳到單期編輯頁，複查就在那裡進行，不再有第二套介面。左側目錄圖、右側逐篇 inline 編輯（含以名稱輸入的遊戲與標籤），可在任一列上下插入、可刪除、可拖曳排序
- **期刊瀏覽頁重做**——刊頭（masthead）與藏書照分開存，單期列表改成書架式封面牆、可切換排序方向；列表頁標出收錄本數，可依分類（PC Game／TV Game／Online Game）篩選、依名稱或創刊日排序。**沒有任何刊物的分類不顯示 chip**——擺一個永遠是 0 的按鈕等於給讀者一個按了不會有事的控制項
- **遊戲索引**——每頁 40 款、寬螢幕四欄，搜尋／篩選／排序全部走網址。只提供「名稱」與「相關文章數」兩個維度，因為其餘欄位還沒有資料
- **合併重複的遊戲條目**——後台列表上的按鈕與 `scripts/merge-game.ts` 共用同一份邏輯，兩邊留下的紀錄才會長一樣。敗方的名稱留成 alias，不然合併等於把一種寫法從索引上抹掉
- **單期的其他編號**——一期常常同時印著好幾個號碼：《電玩通》封面上有日期式的發行號、年度內編號與香港版卷號，封底還有總號，而 `issueNumber` 只裝得下一個。改存成 `altNumbers` 字串陣列，因為有幾個會隨期別變動
- **貢獻者自己的 API token**——在 `/admin/profile` 產生，拿它寫入等同本人操作：紀錄署他的名，權限讀他當下的 role，所以降級或刪帳號就等於撤銷他所有的 token。只存 sha256，明碼在產生當下顯示一次
- **匯出紀錄**——每次匯出留下匯出者、範圍、筆數與 IP，`/admin/export-logs` 僅 ADMIN 可見。紀錄在第一個位元組送出前就寫下，所以中途斷掉的下載也留得下痕跡
- **貢獻者可以改自己的顯示名稱**——那個名字會出現在排行、活動流與每一筆編輯紀錄上
- **備份**——每晚 03:00（台北）dump 資料庫，每週把掃描圖鏡像到 R2。Neon 免費方案只留六小時歷史，在此之前這個站與「一次誤刪」之間什麼都沒有
- **preview 部署有自己的資料庫**，不再對著正式資料試東西
- **sitemap.xml 與 robots.txt**——動態產生，dev 實測 1157 條網址；大部分內容藏在兩層路徑底下，先前沒有任何東西告訴爬蟲它們在哪
- **從 Google Sheet 匯入單期的腳本**，以及後台側欄的待複查徽章

### 變更

- **遊戲索引預設依相關文章數排序**（由多到少）——A-to-Z 的第一頁等於一頁沒人寫過的遊戲，624 款裡有 481 款只被提過一次
- **貢獻的定義收斂成編目工作**——帳號變更仍留在稽核紀錄裡，但不計入排行與活動流，否則把名字改過去再改回來就能白賺兩次貢獻；匯入腳本同樣不列入
- **批次儲存改成每筆記錄各留一行**，共用 `batchId`，活動流再以一行帶過整批。先前只寫一筆摘要，除了第一筆以外的記錄都沒有自己的來歷，事後補不回來
- **刪除紀錄記得下名字**——紀錄活得比資料久，只留 id 的話事後看不出刪掉的是什麼
- **首頁改依最後更新排序**，格線加寬，**封面一律完整顯示不裁切**——雜誌封面滿是刊名與標題文字，填滿式縮放會把字切掉
- **chip 全站統一**成一份配色與圖示表；允許的圖片格式清單也從三處收斂進 `image-policy.ts`
- ESLint 的 warning 在 CI 視同錯誤

### 修正

- **後台遊戲列表只取 100 筆，卻把那個數字當成總數**顯示
- **期刊列表把刊頭切掉**——顯示的是每張 masthead 的一段
- **中文 slug 的百分比編碼**——沒有編碼就不是合法網址，而那正是這個站大部分內容所在的層
- **所有時間改以台北時間顯示**——`date-fns` 的 `format()` 跟著執行環境的時區跑，同一筆編輯在 `/admin/edit-logs` 讀 03:20、在 `/admin/users` 讀 11:20
- **後台單期頁的 hydration mismatch**——每個 `DndContext` 補上明確的 id
- **沒有改到任何欄位的儲存不再寫編輯紀錄**——那會在活動流留一行、在排行加一分，而沒有人改過任何東西
- **編輯紀錄的寫入改為 await**——先前留著不管，serverless 上函式可能在它抵達資料庫之前就被凍結
- **上傳的圖片在瀏覽器端先縮**，以符合 Vercel 的 body limit，塞不下時會說為什麼
- **部分更新會讓建立時的預設值復活**——`.partial()` 擋不住陣列欄位的 `@default`
- **RAWG 封面抓失敗會讓使用者看到**，沒設 key 時直接不顯示那個按鈕
- `sharp` 的 libvips 二進位隨上傳路由一起打包；Vercel 上不再輸出 standalone；`sslmode` 釘成它今天的語意

### 安全性

- **會花錢或寫檔的路由自己再檢查一次授權**——授權集中在 middleware 是對的，問題是它是**唯一**一層，而 Next.js 出過 middleware 繞過的 CVE（CVE-2025-29927），繞過去換來的是一次模型帳單
- **圖片來源白名單改綁在設定的 origin 上**，不再跟著請求的 host 走

## [0.3.0] - 2026-08-13

這一版的重心是把 OCR 之後的**人工複查流程**補完，讓一期的目錄有明確的「誰確認過、什麼時候確認的」，並修掉一批安全性與效能問題。

### 新增

- **單期複查流程**——單期新增 `tocReviewedAt`，記錄是否有人確認過目錄；`/admin/issues` 列出尚未複查的單期。人工批次儲存文章時才會標記，**透過 API token 的寫入不標記**，因為無人值守的流程不能宣稱有人檢查過
- **複查頁自動載入辨識結果**——`ocr_records` 先前是只寫不讀的表，辨識結果存了卻沒有任何介面讀得回來。新增 `GET /api/issues/[id]/ocr` 取最新一筆，複查時不必重跑模型
- **編輯紀錄瀏覽介面**——`/admin/edit-logs` 供 ADMIN 依使用者、類型、動作篩選；紀錄同時保存變更前後的值，並解析出每筆紀錄指向的對象名稱
- **貢獻者頁**——前後台各一，顯示貢獻排行與近期活動
- **EDTF 日期**——期刊的創刊／停刊日與單期的出版日改存 EDTF（ISO 8601-2），可以表達「1994」「1999-05」「1994-22」這種只知道年、月或季的情況，不必虛構日期。另存 `foundedSort` / `publishSort` 供排序
- **批次寫入用的 API token**——腳本可用 Bearer token 認證寫入，紀錄歸屬於「司書(NPC)」帳號。token 不能用於使用者管理
- **支援 OpenAI 相容端點**——設定 `OPENAI_BASE_URL` 即可指向自架的模型服務，並可讓推理型模型跳過思考鏈
- **搜尋擴及期刊與遊戲**，首頁改以搜尋為主
- **上傳自動縮圖與轉檔**——顯示用圖縮到長邊 1600 並轉 WebP；目錄頁掃描圖縮到 2400 並**維持 JPEG**
- **CSV 匯出**改為串流輸出，單期以 50 筆為一批讀取
- **持續整合**——每個 PR 跑 ESLint、`tsc --noEmit` 與 Jest
- **效能量測**——`PERF_LOG` 可開關的伺服器端計時，用來定位後台延遲
- 目錄圖示的 favicon、`BACKLOG.md`、以及雜誌匯入的資料慣例文件

### 變更

- **文章分類改為 Prisma enum**——原本是自由字串，同一個分類名被改過兩次，每次都要寫資料 migration。中文標籤現在放在 `src/lib/article-categories.ts`，改字不動資料。分類詞彙也統一成單字詞
- **辨識信心度不再上色**——原本依信心度標綠／黃／紅，但收集到的樣本全是滿分，等於永遠綠燈，讀起來像「已檢查、沒問題」而其實沒有。改為只顯示數值
- **OCR 複查介面多次調整**——欄位改用雜誌實際的說法命名、掃描圖完整可見、列動作不再佔掉一整條空白、標籤顯示其類型、長清單可操作
- **README 依現況重寫**，參考資料拆成 `docs/features.md`、`docs/routes.md`、`docs/architecture.md`
- Prisma Client 改在 install 時生成

### 修正

- **後台每頁 1.8–2.8 秒的延遲**——函式在 `iad1`、資料庫在新加坡，每次請求都跨太平洋往返。將函式區域釘在 `sin1` 後，暖機後為 13–50ms
- **首頁「最新單期」會撈到空殼期數**——既無封面也無文章的單期看起來像壞掉，現已濾除
- **部分更新會把有預設值的欄位重設**——PATCH 只帶部分欄位時，未帶到的欄位被寫回預設值
- **空白的表單欄位被存成空字串**而非 null
- **移除期刊 ISSN 的唯一性限制**——改名可以沿用同一組 ISSN，《電擊王》與《電玩通》就共用 1561-8099
- **目錄頁掃描圖不再轉 WebP**——自架的模型後端解不開 WebP，會回 500
- **分頁參數加上邊界**——`?page=abc` 會以 NaN 進到 Prisma 並回 500，`limit` 則沒有上限，一個請求就能要求整張表
- **匯出的「沒有單期的雜誌」少一欄**——該列只有 19 欄而表頭是 20 欄，嚴格的 CSV parser 會報錯或整列錯位
- OCR 路由的執行時間上限設為 60 秒；trgm 索引的 migration 移除 `CONCURRENTLY`；Prisma schema 補上 SQL migration 已建立的索引；後台回前台的入口不再有歧義
- 清掉一個長期存在的 ESLint error 與三個 tsc error，使兩者都能作為關卡

### 安全性

- **`DEV_BYPASS_AUTH` 在 production 一律無效**——這個旗標會跳過 middleware 的所有檢查，而那是唯一一層授權；Vercel 的環境變數凍結在部署當下，誤設要 redeploy 才收得回來
- **CSV 匯出的 formula injection**——文章標題直接來自 OCR，而匯出檔就是要給 Excel 開，以 `=` `+` `-` `@` 開頭的欄位會被當成公式執行
- **匯出需要登入**——middleware 原本只擋寫入，這支一個請求回傳整份目錄的端點任何人都打得到
- **OCR 不再回傳上游錯誤訊息**——自架模型後端的錯誤可能含內部 URL 與組態
- **OCR 單次請求限制 10 張圖**——原本收多少送多少，一次 50 張就是 50 張的模型帳單

## [0.2.0] - 2026-04-01

### 新增

- **編輯稽核軌跡**——所有寫入 API 都留下 `EditLog`
- **貢獻者追蹤與排行**
- **全文搜尋**——加上 `pg_trgm` 索引並擴大搜尋範圍
- **文章列表 inline 編輯**與批次建立
- **CSV 匯出**
- **遊戲封面**（RAWG 搜尋帶入）、遊戲列表分頁、智慧標籤建議、inline 新增
- 後台各頁的返回按鈕、OCR 進度指示

### 變更

- 介面用詞由「期數」改為「單期」
- 前台的 `<img>` 改用 `next/image`
- 抽出共用的 `StatGrid` 元件與 `withErrorHandler`，消除各路由重複的 try/catch
- 以 ISR 取代 `force-dynamic`，並將首頁查詢並行化
- 改善 OCR prompt，抽出共用的解析邏輯
- 前後台大幅的 UX 調整：間距、卡片、響應式格線、後台側欄（行動裝置抽屜、640px 以上常駐）

### 修正

- **OCR 圖片抓取的 SSRF 防護**——僅允許同源與白名單的儲存網域
- **OCR API 加上速率限制**
- `@prisma/client` 從 devDependencies 移到 dependencies

### 測試

- 以 Prisma mock 新增 API 路由的整合測試

## [0.1.0] - 2026-02-13

第一個可用的版本。

### 新增

- **專案基礎**——Next.js 16 App Router、TypeScript、Prisma、PostgreSQL
- **期刊與單期 CRUD**，期刊詳情頁三欄佈局、可搜尋下拉選單
- **單期拖曳排序**——@dnd-kit，樂觀更新並在失敗時回滾
- **AI 目錄辨識**——Claude Vision API 起家，以策略模式與工廠模式支援 Claude / OpenAI / Gemini 三種 Provider，並依環境變數動態偵測可用服務
- **多圖辨識**——一期可有多張目錄頁，OCR 頁自動帶入已上傳的圖片
- **雙欄複查編輯器**——左側圖片預覽、右側文章列表 inline 編輯
- **文章編輯**——個別編輯與批次建立
- **標籤與遊戲**——標籤六種類型，兩者的詳情頁都依期刊、單期分組顯示相關文章，可展開收合
- **前台頁面**——首頁、搜尋、期刊／遊戲／標籤瀏覽
- **認證與權限**——Google OAuth 登入，VIEWER／EDITOR／ADMIN 三種角色，首位使用者自動成為 ADMIN
- **開發模式免登入**——`DEV_BYPASS_AUTH=true`
- **CSV 批次匯入**期刊與單期
- **本地檔案儲存 fallback**——沒有 Blob token 時寫入 `public/`
- **單元測試**——涵蓋所有 validator 與工具函式
- **部署支援**——Dockerfile、docker-compose.yml、Vercel 部署文件

### 修正

- OCR 路由的相對圖片 URL 解析
- 上傳的 placeholder token 判斷
- `SelectItem` 空字串值造成的執行期錯誤

[未發布]: https://github.com/tzengyuxio/tocr/compare/v0.7.0...HEAD
[0.7.0]: https://github.com/tzengyuxio/tocr/compare/v0.6.0...v0.7.0
[0.6.0]: https://github.com/tzengyuxio/tocr/compare/v0.5.0...v0.6.0
[0.5.0]: https://github.com/tzengyuxio/tocr/compare/v0.4.0...v0.5.0
[0.4.0]: https://github.com/tzengyuxio/tocr/compare/v0.3.0...v0.4.0
[0.3.0]: https://github.com/tzengyuxio/tocr/compare/v0.2.0...v0.3.0
[0.2.0]: https://github.com/tzengyuxio/tocr/compare/v0.1.0...v0.2.0
[0.1.0]: https://github.com/tzengyuxio/tocr/releases/tag/v0.1.0
